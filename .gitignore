# Environment variables
.env*

# Rust / Cargo
target/
Cargo.lock
# Ignore intermediate artifactscargo check --future-incompat-report
# **/*.rs.bk

# Node.js / npm / yarn / pnpm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# Lockfiles (optional, depending on team policy)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Tauri / Frontend build artifacts
dist/
dist-tauri/
src-tauri/target/ # Explicitly ignore Tauri's Rust target dir if nested

# IDE specific files
.idea/
.vscode/
*.iml
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
# Mac
.DS_Store
.AppleDouble
.LSOverride
# Thumbnails
._*
# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
# Windows
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
[Dd]esktop.ini
$RECYCLE.BIN/
*.lnk

neo4j_data/